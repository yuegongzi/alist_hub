spring:
  datasource:
    url: jdbc:sqlite:/Users/mac/.alist_data/data.db
    driver-class-name: org.sqlite.JDBC
  jpa:
    show-sql: true
logging:
  level:
    web: DEBUG

alist:
  hub:
    path: /Users/mac/.alist_data